install: gradle wrapper --gradle-version 5.5.1
language: java
sudo: false
dist: trusty
cache:
  directories:
    - $HOME/.m2
jdk:
- oraclejdk8
script: ./travis/build.sh
before_install:
- chmod +x ./travis/build.sh

